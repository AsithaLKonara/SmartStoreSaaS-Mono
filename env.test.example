# Test Environment Configuration
# Copy this to .env.test for testing
# Run: npm run test:env

NODE_ENV=test
NEXT_PUBLIC_APP_NAME=SmartStore SaaS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (Use separate test database)
# For local PostgreSQL:
DATABASE_URL="postgresql://smartstore:smartstore123@localhost:5432/smartstore_test"
# For Docker PostgreSQL (if using docker-compose):
# DATABASE_URL="postgresql://smartstore:smartstore123@localhost:5432/smartstore_test"

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=test-secret-key-change-in-production-min-32-chars-smartstore-saas-e2e-test

# Mock External APIs
MOCK_EXTERNAL_APIS=true

# Email (Mock)
EMAIL_FROM=noreply@smartstore.test
SENDGRID_API_KEY=mock_sendgrid_key

# SMS/WhatsApp (Mock)
TWILIO_ACCOUNT_SID=mock_twilio_sid
TWILIO_AUTH_TOKEN=mock_twilio_token

# Payments (Mock)
STRIPE_SECRET_KEY=sk_test_mock
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_mock

# JWT Secrets (for testing)
JWT_SECRET=test-jwt-secret-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-change-in-production-min-32-chars

# Test credentials (will be created by seed script)
TEST_USER_EMAIL=admin@smartstore.com
TEST_USER_PASSWORD=admin123

# E2E Test Configuration
E2E_BASE_URL=http://localhost:3000



